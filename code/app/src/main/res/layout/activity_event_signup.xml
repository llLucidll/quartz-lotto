<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/signupScrollView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_background">

    <LinearLayout
        android:id="@+id/signupLinearLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Event Poster -->
        <ImageView
            android:id="@+id/signupPosterImageView"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_placeholder_image" />

        <!-- Event Name -->
        <TextView
            android:id="@+id/signupEventNameTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Event Name"
            android:textAppearance="?android:attr/textAppearanceLarge"
            android:paddingTop="8dp" />

        <!-- Date -->
        <TextView
            android:id="@+id/signupDateTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Date: "
            android:paddingTop="4dp" />

        <!-- Time -->
        <TextView
            android:id="@+id/signupTimeTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Time: "
            android:paddingTop="4dp" />

        <!-- Description -->
        <TextView
            android:id="@+id/signupDescriptionTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Description"
            android:paddingTop="8dp" />

        <!-- Max Attendees -->
        <TextView
            android:id="@+id/signupMaxAttendeesTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Max Attendees: "
            android:paddingTop="4dp" />

        <!-- Current Attendees -->
        <TextView
            android:id="@+id/signupCurrentAttendeesTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Currently Waiting: "
            android:paddingTop="4dp" />

        <!-- Sign Up Button -->
        <Button
            android:id="@+id/signupButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Sign Up"
            android:layout_marginTop="16dp"
            android:backgroundTint="@color/avatar_color_2"/>

        <!-- Progress Bar -->
        <ProgressBar
            android:id="@+id/signupProgressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="16dp" />

    </LinearLayout>
</ScrollView>