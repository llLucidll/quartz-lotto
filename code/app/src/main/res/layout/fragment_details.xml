<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/detailsScrollView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@drawable/gradient_background">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Event Name -->
        <TextView
            android:id="@+id/detailsEventNameTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Event Name"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="#000000" />

        <!-- Poster Image -->
        <ImageView
            android:id="@+id/detailsPosterImageView"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:layout_marginTop="16dp"
            android:contentDescription="Event Poster"
            android:src="@drawable/ic_placeholder_image" />

        <!-- Date -->
        <TextView
            android:id="@+id/detailsDateTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Date: MM/DD/YYYY"
            android:textSize="18sp"
            android:textColor="#757575"
            android:layout_marginTop="16dp" />

        <!-- Time -->
        <TextView
            android:id="@+id/detailsTimeTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Time: HH:MM"
            android:textSize="18sp"
            android:textColor="#757575"
            android:layout_marginTop="8dp" />

        <!-- Description -->
        <TextView
            android:id="@+id/detailsDescriptionTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Event Description"
            android:textSize="16sp"
            android:textColor="#757575"
            android:layout_marginTop="8dp" />

        <!-- Max Attendees -->
        <TextView
            android:id="@+id/detailsMaxAttendeesTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Max Attendees: 100"
            android:textSize="16sp"
            android:textColor="#757575"
            android:layout_marginTop="16dp" />

        <!-- Max Waitlist -->
        <TextView
            android:id="@+id/detailsMaxWaitlistTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Max Waitlist: 50"
            android:textSize="16sp"
            android:textColor="#757575"
            android:layout_marginTop="8dp" />

        <!-- Geolocation -->
        <TextView
            android:id="@+id/detailsGeolocationTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Geolocation: Enabled"
            android:textSize="16sp"
            android:textColor="#757575"
            android:layout_marginTop="8dp" />

        <!-- QR Code -->
        <ImageView
            android:id="@+id/detailsQrCodeImageView"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_gravity="center"
            android:layout_marginTop="24dp"
            android:visibility="gone"
            android:contentDescription="QR Code"
            android:src="@drawable/ic_placeholder_image" />

        <!-- Progress Bar -->
        <ProgressBar
            android:id="@+id/detailsProgressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="24dp"
            android:visibility="gone" />

        <Button
            android:id="@+id/detailsUpdatePosterButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/avatar_color_4"
            android:text="Update Event Poster" />

    </LinearLayout>
</ScrollView>
